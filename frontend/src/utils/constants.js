// Base URL of your backend — change this if your port is different
export const BASE_URL = "http://localhost:5000";

// These are the exact role strings your backend uses (lowercase)
export const ROLES = {
  ADMIN: "admin",
  USER: "user",
};