import AppRouter from "./router/index";

// App.jsx is the root component
// It just renders the router — all layout and pages live inside routes

export default function App() {
  return <AppRouter />;
}
